extends layout
block content
   .container.landing
    .row
        .col.s12.push-s1
            .row
                .col.m12
                    h1 #[i.fa.fa-exclamation-circle] #{error.status}
                    h3= message
                    p= error.stack